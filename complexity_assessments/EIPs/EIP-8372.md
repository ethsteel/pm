### Checklist

| Anchor | Score (0–3) | Rationale |
|---|---:|---|
| **EVM Gas rule changes** | 3 | The block-level rule changes: raw state gas answers to a scaled limit and is normalized before `gas_used = max(execution, normalized state)`. Measured: an EELS prototype (proportional contraction, `CPSB` 765 / scale 50) flips 796 fixture executions across the EIP-8037/7778/2780/8038 gas suites and the ported statics. |
| **State-access ordering within opcode execution** | 0 |    |
| **Blob gas accounting changes** | 0 |    |
| **State gas accounting changes** | 2 | The block-level state-gas budget is modified (scaled to a share of the block gas limit) and `CPSB` recalibrates with it; the spill path and charging sites are untouched. |
| **New EVM gas refund** | 0 |    |
| **Patterns affecting pre-existing tests** | 2 | Measured: 796 executions across 155 functions — all state-gas-exact tests (header pins of raw state gas, capacity-bound scenarios, 1530-era ported values); a considerable but well-scoped category. Sixteen shared header sites were updated fork-correctly; the remainder re-derives when the real calibration constants are chosen. |
| **New invariant on pre-existing tests** | 0 |    |
| **Transition-tool interface changes** | 0 |    |
| **New test-framework primitives** | 1 | Existing primitives extend: `block_state_gas_limit()` / `normalized_block_state_gas()` fork accessors (identity pre-EIP), and the framework's implicit transaction gas limit learns the scaled capacity bound. |
| **Cryptography** | 0 |    |
| **Edge/boundary conditions** | 2 | The scaled-capacity admission boundary, integer normalization rounding, and the dominant-dimension switchover are each boundary-prone, but none needs an elevated case count. |
| **Block syncing changes** | 0 |    |
| **Engine API changes** | 0 |    |
| **Added system contracts** | 0 |    |
| **Modified system contracts** | 0 |    |
| **Added opcodes** | 0 |    |
| **Modified opcodes** | 0 |    |
| **Added precompiles** | 0 |    |
| **Modified precompiles** | 0 |    |
| **Encoding changes (RLP/SSZ)** | 0 |    |
| **New transaction types** | 0 |    |
| **New or modified transaction validity mechanisms** | 2 | With a scale below 100, the EIP-8037 admission rule (`tx.gas <= state_gas_available`) silently caps every transaction's gas limit at the scaled share of the block gas limit — an unstated consequence, and the dominant blast-radius mechanism until tooling learns the bound (31,873 fixture executions in the prototype's first fill). Existing validity tests need limited, mechanical updates. |
| **New block / header fields** | 0 |    |
| **New fork activation mechanism** | 0 |    |
| **Performance risks** | 0 |    |
| **Security risks** | 1 | Pure arithmetic over existing counters, validated in isolation; the real risk is parameter miscalibration, which is an economic failure mode fixed only by a later fork — flagged by the EIP itself. |
| **Unspecified behavior requiring cross-client consensus** | 1 | Both constants are `TBD` pending the calibration methodology; the mechanism itself is fully specified, and the prototype's obvious readings (including the admission-cap consequence) held without interpretation disputes. |
| **Cross-EIP interactions** (uncapped) | 2 | A delta on EIP-8037's block accounting, interacting with EIP-7825 (the admission-cap consequence, measured) and EIP-7778's block-accounting suites — limited, mechanical coordination. |

**Total: 16**

#### Special Considerations

The admission-cap consequence deserves a line in the EIP: any scale below 100 makes `min(block_gas_limit x scale%, TX_MAX_GAS_LIMIT + reservoir)` the effective per-transaction ceiling, which test tooling and wallet gas estimation must both learn.

#### Notes

Scores calibrated against a full EELS prototype (proportional contraction calibration, both spec and tooling normalization paths implemented); the full-suite fill measures 796 flips, all diagnosed, zero remaining failures after fork-correct updates and parking.

#### Final Assessment

| Category | Description | Value |
|-----------|--------------|:----:|
| **Total Score** | Sum of all anchor scores (0–84 nominal; **Cross-EIP interactions** is uncapped, so there is no hard maximum) | **`16`** |
| **Complexity Tier** | Computed from total score | 🟡 |

##### Tier Interpretation

| Tier | Range | Meaning |
|------|--------|----------|
| 🟢 **Low Complexity** | **<12** | Minor feature or localized change. Existing tests are largely unaffected. Does not require intensive cross-EIP testing. |
| 🟡 **Medium Complexity** | **>=12<23** | Moderate change affecting multiple components. Requires moderate cross-EIP testing. |
| 🔴 **High Complexity** | **>=23** | Broad or deep impact on protocol behavior; high regression risk; and/or requiring intensive cross-EIP testing. |
